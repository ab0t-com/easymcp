# EasyMCP Examples

This directory contains public example configs only. It intentionally excludes private implementation code.

- `server/*.yaml` and `server/*.json` — EasyMCP/OpenAPI example configs.
- `cli/instances.example.yaml` — sample CLI instance registry.
